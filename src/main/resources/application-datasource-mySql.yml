spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        initialization-mode: always
        password: root
        url: jdbc:mysql://localhost:3306/restful_webservice?useSSL=false
        username: root
